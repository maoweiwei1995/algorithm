/**
 * @param {string[]} words
 * @param {string} chars
 * @return {number}
 */
var countCharacters = function(words, chars) {
  let temp = ''
  let hash = {}
  for (let i = 0; i <chars.length; i++) {
      
  }
  for (let i = 0, len1 = words.length; i < len1; i++) {
    let temp = words[i]
    for (let j = 0; j < temp.length; j++) {
        if ()
    }
  }  
};