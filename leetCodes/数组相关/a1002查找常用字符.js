/**
 * @param {string[]} A
 * @return {string[]}
 */
var commonChars = function(A) {
    let len = A.length
    let ans = []
    for (let j = 0; j < A[0].length; j++){
        let temp = A[0][j]
        for (let i = 1; i < A; i++){
            if ()
        }
        ans.push(temp)
    }
};