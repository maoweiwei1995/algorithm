/**
 * @param {number} N
 * @param {number} K
 * @return {number}
 */
var kthGrammar = function(N, K) {
    let str = '0'  
    if(N > 0) {
        digui(N-1, K)
    }
};
function digui(str, n, k){
    let temp = 1
    for (let i = 0; i < n; i++)
    let str2 = parseInt(str, 2) ^ 
    if (n > 0){
        digui(str, n-1, k)
    }
}